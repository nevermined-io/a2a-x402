[project]
name = "x402-a2a"
version = "1.0.0"
description = "Official x402 payment protocol extension for A2A"
readme = "README.md"
license = { file = "LICENSE" }
authors = [
    { name = "Coinbase Developer Platform", email = "x402@coinbase.com" },
    { name = "Google A2A", email = "a2a@google.com" }
]
requires-python = ">=3.11"
keywords = ["a2a", "x402", "payments", "ethereum", "coinbase", "google"]
dependencies = [
    "a2a-sdk",  # For A2A protocol integration
    "x402>=0.2.0",  # For x402 payment protocol
    "payments-py>=0.1.0",  # Nevermined payments SDK with x402 module
    "eth-account>=0.13.7",  # For Ethereum account management
    "httpx>=0.28.1",  # For async HTTP client
    "pydantic>=2.0.0",  # For data validation
    "pytest>=9.0.3",  # For testing
    "pytest-asyncio>=1.1.0",
]

[dependency-groups]
dev = [
    "pytest-cov>=6.2.1",
    "pytest-mock>=3.14.1",
    "trio>=0.22.0",
]
lint = [
    "ruff>=0.13.1",
]

[project.scripts]
test = "pytest:main"

[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
# Tell setuptools that the x402_a2a package is in the current directory
packages = ["x402_a2a", "x402_a2a.core", "x402_a2a.executors", "x402_a2a.types"]
package-dir = {"x402_a2a" = "."}

[tool.setuptools.package-data]
x402_a2a = ["py.typed"]

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "unit: Unit tests",
    "integration: Integration tests", 
    "async: Async tests"
]

[tool.ruff]
target-version = "py311"

# Development: Use local editable payments-py for development
# Production: Will use published payments-py from PyPI
[tool.uv.sources]
# Uncomment for local development:
# payments-py = { path = "../../../payments-py", editable = true }
